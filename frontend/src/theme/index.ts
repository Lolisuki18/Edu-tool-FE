/**
 * EduTool Design System - Main Export
 * Central access point for all design tokens
 */

export { COLORS, CSS_VARS, type ColorKey } from './colors';
export { TYPOGRAPHY, TYPOGRAPHY_VARS } from './typography';

// Spacing constants (optional, if you want to use in JS)
export const SPACING = {
  section: '3rem',
  card: '2rem',
} as const;
